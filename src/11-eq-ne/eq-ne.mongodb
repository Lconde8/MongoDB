use("platzi_store")

//Using $eq (is use implicity)
//db.inventory.find({ qty: 20 })
//db.inventory.find({ qty: { $eq: 20} })
//db.inventory.find({ "item.code": "123"})

//Using $ne (is use explicit)
db.inventory.find({ qty: { $ne: 20 } })