use("platzi_store")

/*db.inventory.updateMany(
    {},
    { $pull:{
        tags: "book"
        }
    }    
)*/

//eliminar mas de un elemento en el atributo del documento
db.inventory.updateMany({},
    {
        $pull: {
        tags: {
            $in: ["appliance", "school"]
            }
        }
    }
)